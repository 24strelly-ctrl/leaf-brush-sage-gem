{
  "buildCommand": "npm run build",
  "outputDirectory": ".vercel/output",
  "framework": "vite",
  "regions": ["iad1"],
  "name": "eternity-calls",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "build": {
    "env": {
      "DATABASE_URL": ""
    }
  }
}